int removeDuplicates(int* nums, int numsSize) {
    if (numsSize <= 2) return numsSize; 
  int c=2;
  for(int i=2;i<numsSize;i++){
    if( nums[i]!=nums[c-2]){
        nums[c]=nums[i];
        c++;
    }
  }
  return c;
}
